{
  "name": "stf-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:install": "playwright install chromium --with-deps"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "serve": "^14.2.4"
  }
}
