{
  "name": "Sensor Tattoo Fix (3N20)",
  "install": "npm i -g wrangler@4\ncd api && (npm ci --prefer-offline --no-audit --no-fund 2>/dev/null || true)",
  "start": "chmod +x scripts/cloud-deploy.sh 2>/dev/null || true"
}
