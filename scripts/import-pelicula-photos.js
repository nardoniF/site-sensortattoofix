#!/usr/bin/env node
/**
 * @deprecated Use fetch-pelicula-listing-photos.js (anúncios na internet).
 * Este script copiava embalagens IMG_* — não usar para o site.
 */
console.error('Use: node scripts/fetch-pelicula-listing-photos.js');
process.exit(1);
