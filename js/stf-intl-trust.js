/**
 * Trust line below the hero badge — removed (was Secure checkout · Tracked shipping · email).
 * File kept so old HTML that still loads this URL does not 404 or reinject the strip.
 */
(function () {})();
